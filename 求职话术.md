# 求职面试话术手册

---

## B — 工程化基础

### B9：CI 流程

我的项目配置了完整的 CI 流程。
每次代码推送，GitHub Actions 都会自动拉起一个干净的 Python 环境，安装依赖，
然后依次执行 `ruff` 做**代码**检查，`mypy` 做**类型**检查，
最后用 `pytest` 运行所有单元测试并生成覆盖率报告。这是保证代码质量的关键工程实践。

### B10：Docker 容器化

> 我为项目写了 Dockerfile，通过容器化技术把运行环境固定下来。这样不管在本地、测试环境还是生产环境，都能用同样的方式运行，彻底解决了"在我电脑上明明能跑"的问题。

---

## C — 技术栈实践

### C1：API 客户端封装

> 我写了一个 API 客户端，封装了 GET 和 POST 请求，支持错误处理、超时设置和重试机制。这样在调用外部 API 时，调用方只需要关心业务逻辑，不需要重复处理网络问题。这也是我在项目中常用的模块化思路。

**补充：**

> 我在项目中封装了 API 客户端，支持 GET、POST、PUT、DELETE 方法，使用 Session 复用连接，并通过 headers 传递认证信息。我还加入了超时和重试机制，保证 API 调用的稳定性。

### C2：数据解析（JSON）

> JSON 是前后端数据传输的标准格式。我熟悉 Python 的 `json` 模块，能将 Python 对象序列化为 JSON 字符串，也能解析 JSON 数据为 Python 对象，还能将数据持久化到文件。这在调用大模型 API 时非常常用。

### C3：文本清洗（pandas）

> 我熟悉用 `pandas` 处理 CSV 和 Excel 数据，能做数据清洗、字段提取、格式转换。在 RAG 项目中，我经常用 `pandas` 分析检索结果、统计评估指标。

> 我用 `pandas` 处理过 CSV 数据，包括读取、清洗、筛选、分组统计和新增列。在 RAG 项目中，我常把评估结果保存为 CSV，用 `pandas` 分析不同模型的表现差异。

### C4：正则表达式

> 正则表达式是文本处理的必备工具。我在数据清洗和文本预处理中经常用它来提取和验证特定格式的数据。

> 我熟练使用正则表达式处理文本数据，包括提取手机号、URL、日期和话题标签，以及清理多余空格。在 RAG 项目中，我用正则从文档中提取关键信息，比如标题、作者、日期等元数据。

### C5：PDF 处理

> 我在 RAG 项目中使用 `pypdf` 处理 PDF 文档，提取文本、分页保留元数据。这是 RAG 链路的第一步，直接影响后续的检索质量。

> 我使用 `pdfplumber` 处理 PDF 文档，它能提取文本、表格和元数据。对中文 PDF，我会确保使用支持中文的字体。在 RAG 项目中，我把 PDF 按页提取成结构化 JSON，保留页码和元数据，方便后续检索。

### C6：并发处理

> 在 RAG 项目里，批量处理文档是常见需求。我用 `concurrent.futures` 实现并发处理，把 100 个 PDF 的处理时间从 10 分钟缩短到 2 分钟，而且代码很简洁。

> 我使用 `concurrent.futures.ThreadPoolExecutor` 做并发处理。在批量处理文档时，我会把所有任务提交到线程池，用 `as_completed` 按完成顺序获取结果。这样既能提升效率，又能实时跟踪每个任务的进度。对于 I/O 密集型任务（文件读取、API 调用），用线程池效果很好。

### C7：API 构建（FastAPI）

> 我使用 FastAPI 构建 Web API 服务，它基于现代 Python 类型注解，自动生成 OpenAPI 文档，性能对标 Node.js。在 RAG 项目中，我用它提供问答、检索和文档上传接口。

```bash
uvicorn src.api:app --reload --host 127.0.0.1 --port 8000
```

### C8：数据库管理（SQLAlchemy + SQLite）

> 我使用 SQLite 和 SQLAlchemy 在项目中管理数据，能完成表设计、增删改查和关联查询。在 RAG 系统中，我用数据库存储文档元数据、向量索引信息和用户查询日志，方便后续分析和优化。

> 我使用 SQLAlchemy ORM 管理 SQLite 数据库，通过 Python 类定义数据模型。在项目中，我设计了文档表和查询日志表，支持增删改查、关联查询和统计。这让我能追踪文档处理历史和查询效果，为系统优化提供数据支持。

---

## 💡 你的主要收获（可写入简历）

### 从"脚本编写"到"工程化工具开发"

你已能使用 `pandas`、`pdfplumber`、`requests` 等主流库，构建处理 PDF、Excel、CSV 及 API 数据的可复用工具模块。

### 从"本地脚本"到"API 服务提供"

你使用 FastAPI 将业务逻辑封装为标准 HTTP 接口，并配合 Swagger 自动生成文档，实现了从"功能"到"服务"的跨越。

### 从"内存处理"到"数据持久化"

通过 SQLAlchemy + SQLite，你已能将处理结果结构化存储，并通过 ORM 进行管理，为后续构建有状态的复杂应用打下基础。

### 建立"工程化代码"意识

你坚持了 Python 3.12+ 的现代类型注解，并处理了与 `mypy` 的集成，代码的健壮性和可维护性显著提升。

---

## 🤖 模型选型话术

### 选用 DeepSeek

项目选用 DeepSeek API 作为主大模型，核心原因是业务以中文知识库为主，DeepSeek 在中文长文档、行业术语理解上优于 GPT 系列；同时国内直连无网络壁垒，开发迭代效率更高，配套的专用 Embedding 与重排模型可以快速搭建完整粗排 + 精排 RAG 链路。我同时基于 Ollama 做本地开源模型对比，验证云端与本地部署的效果差异，形成完整评估报告。

### Ollama 补充亮点

为降低 API 调用成本、保护本地测试文档隐私，我基于 Ollama 搭建本地推理服务，复用同一套 LLM 客户端代码，实现云端 / 本地模型无缝切换，对比了不同量化参数、模型尺寸对召回精度、回答幻觉的影响，沉淀了 Chunking 窗口调优经验。

---

## D — RAG 系统

### D2：Prompt 设计

> 在设计 Prompt 时，我会注意四点：**明确角色、具体任务、约束条件和输出格式**。比如，我会先设定 system 提示词定义角色，再通过 user 提示词描述任务。这样能让模型更稳定地输出高质量结果。

> 在设计 Prompt 时，我通常用 system 提示词定义模型角色，用 user 提示词描述任务。如果需要结构化输出，我会在 system 中指定 JSON 格式，这样模型返回的结果可以直接用于程序处理。比如在 RAG 项目中，我会要求模型返回带引用的回答，用 JSON 格式包含回答内容和来源。

### D3：Embedding 向量化

> 我使用 `sentence-transformers` 生成文本的 Embedding 向量，通过余弦相似度计算语义距离。我会根据相似度值自动判断文本之间的关系，比如 0.95 以上是非常相近，0.7 左右是相近，0.2 以下是不相关。在 RAG 系统中，这让我能把用户问题和文档库进行语义匹配，找到最相关的文档片段。

**稀疏向量 vs 稠密向量：**

> 向量表示分为稀疏向量和稠密向量两种。稀疏向量维度高、大部分为 0，适合关键词匹配（如 TF-IDF、BM25）；稠密向量维度低、每个维度都有语义值，适合语义匹配。在 RAG 系统中，我主要用稠密向量做语义检索，但在某些场景下也会结合 BM25 做混合检索，兼顾关键词精度和语义广度。

**混合检索流程：**

```
用户查询
    │
    ├─ 稀疏检索（BM25）→ 关键词匹配 → 候选集 A
    │
    └─ 稠密检索（Embedding）→ 语义匹配 → 候选集 B
                    │
                    ▼
              合并去重 → Rerank → LLM 生成
```

### D4：Chunking 文档切分

> Chunking 是 RAG 系统中最关键的一步。我掌握了固定大小切分和语义切分两种策略，并通过调整 `chunk_size` 和 `overlap` 来优化召回效果。在实际项目中，我会根据文档类型和查询特点选择最合适的切分策略。

> 在 RAG 系统中，Chunking 是关键的第一步。我实现了固定大小切分和语义切分两种策略，通过调整 `chunk_size` 和 `overlap` 来优化召回效果。在检索阶段，我用 Embedding 将查询和所有 chunk 都向量化，通过余弦相似度找到最相关的片段，再把这些片段作为上下文喂给大模型生成回答。

> 检索器是 RAG 系统的核心组件。我先把文档切块并向量化，用户查询时也向量化，然后通过余弦相似度找到最相关的 chunk。这个过程叫语义检索，比关键词匹配更能理解用户意图。

> 在 RAG 系统中，我实现了两种切分策略：固定大小切分和语义切分。固定大小切分通过 `chunk_size` 和 `overlap` 控制，语义切分按段落和句子边界断开。我特别注意了重叠窗口的安全实现——先用步长前进再回退，避免死循环。检索阶段用余弦相似度做语义匹配，`top_k` 参数可以灵活调整召回数量。

### D5：粗排 + 精排（两阶段检索）

> 在 RAG 系统中，我使用了粗排+精排的两阶段检索策略。粗排用 Embedding 从大量文档中快速召回候选，精排用交叉编码器对候选重新打分。这样既能保证检索速度，又能提升精度。重排模型通常比 Embedding 更准确，因为它直接计算查询和文档的交互特征。

**Bi-Encoder（双编码器 / Embedding 召回）：**

```
查询 → Encoder → 向量 → 余弦相似度 → 分数
文档 → Encoder → 向量 ↗

各自独立编码，没有交互
```

**Cross-Encoder（交叉编码器 / Rerank）：**

```
[查询] + [文档] → Encoder → 分数

一起编码，有充分交互
```

**完整 RAG 检索流程：**

```
┌──────────────────────────────────────────────────────┐
│                   RAG 检索流程                        │
├──────────────────────────────────────────────────────┤
│                                                      │
│  用户查询                                            │
│       │                                              │
│       ▼                                              │
│  ┌─────────────────────────────────────────────┐     │
│  │         第一阶段：粗排（召回）                │     │
│  │         速度优先，从海量文档中筛选            │     │
│  ├─────────────────────────────────────────────┤     │
│  │  策略 A：稠密检索（Embedding）               │     │
│  │  策略 B：稀疏检索（BM25/TF-IDF）             │     │
│  │  策略 C：混合检索（A + B）← 最佳实践         │     │
│  └─────────────────────────────────────────────┘     │
│       │                                              │
│       ▼ （候选集缩小到 50-100 个）                    │
│  ┌─────────────────────────────────────────────┐     │
│  │         第二阶段：精排（重排）                │     │
│  │         精度优先，对候选集精细打分            │     │
│  ├─────────────────────────────────────────────┤     │
│  │  策略：Cross-Encoder 重排模型                │     │
│  │        （如 BGE Reranker、Cohere Rerank）    │     │
│  └─────────────────────────────────────────────┘     │
│       │                                              │
│       ▼ （返回 Top-3）                               │
│  ┌─────────────────────────────────────────────┐     │
│  │            LLM 生成最终回答                  │     │
│  └─────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────┘
```

> 我实现了一个完整的 RAG 流程：先用 Embedding 从文档库中召回相关片段，再用 Reranker 对候选进行精排，最后把精排后的上下文拼接进 Prompt，让大模型生成回答。整个过程包含检索、排序、生成三个核心环节。

> 在 RAG 练习中，我用小规模文档演示了完整链路：先把文档切块，用 Embedding 生成向量索引；查询时，用 Embedding 计算语义相似度召回相关块；再用 Reranker 做精排；最后把精排结果拼接成上下文，让大模型生成回答。

### D7：Agent 智能体

```
┌─────────────────────────────────────────────────────────────┐
│                     Agent 工作流程                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  用户输入 ──→ 理解任务 ──→ 选择工具 ──→ 执行工具 ──→ 返回结果 │
│                    │                    │                   │
│                    ▼                    ▼                   │
│              需要什么信息？         调用 API/计算/查询      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**核心概念：**

- **ReAct** = Reasoning + Acting（推理 + 行动）
- **任务拆解**：复杂任务 → 拆成多个子任务 → 逐步完成

> Agent 让 LLM 能够调用外部工具执行具体操作。我实现了两种 Agent：一种是单步 Tool Calling，LLM 会根据用户意图自动选择并调用工具函数，比如查询天气或执行计算，这让 AI 从"对话者"变成了"执行者"；另一种是 ReAct Agent，支持多步推理，ReAct 模式下 LLM 会反复执行「思考 → 行动 → 观察」的循环，直到完成任务。这让 AI 能处理需要多步操作的复杂问题，比如先查天气再对比温度。

### D8：评估与观测

> 我为 RAG 系统设计了评估和观测机制，包括记录每次查询的检索结果、重排结果和生成结果，以及人工评分。这让我能持续发现失败样例，有针对性地优化系统。比如，当发现某个问题的回答质量低时，我会检查检索是否召回了相关文档，重排是否把相关文档排在了前面，从而定位问题。

**Trace 追踪（补充）：**

> 除了记录最终的问答结果，我还对系统做了 Trace 追踪，记录每个请求的检索耗时、重排耗时和生成耗时。这样当用户反馈慢时，我可以快速定位是检索慢、重排慢还是生成慢。Trace 是线上系统调试的必备能力。

**A. Query Trace（查询追踪）**

- 发现检索质量差（候选分数低）
- 发现重排效果差（排序不合理）
- 发现生成慢（latency 高）
- 审计成本和 token 消耗

**B. Ingestion Trace（数据摄入追踪）**

- 发现解析失败（PDF 损坏、字体问题）
- 发现切分不合理（chunk 过小/过大）
- 发现向量化慢（模型性能问题）
- 监控索引构建成本

---

## E — 进阶架构设计

### E3-2：提示词模板管理

🎓 **面试官问："你怎么管理 Prompt？"**

> 我设计了一套提示词模板管理系统，核心思路是**业务逻辑和提示词文本分离**。每个模板有独立的名字、描述和版本号，支持 `{variable}` 占位符动态渲染。这样做有三个好处：
>
> 1. 集中管理、方便复用
> 2. 非技术人员也能调整提示词而不用改代码
> 3. 方便做 A/B 测试对比不同 prompt 的效果
>
> 实际项目中我预置了三类模板：RAG 问答、文档摘要和相关性评估。

### E3-3：多模型切换

🎓 **面试官问："你的项目怎么管理不同模型的调用？"**

> 我在 LLMClient 中实现了模型别名机制。配置文件里定义 `fast → deepseek-v4-flash`、`pro → deepseek-v4-pro`，调用时传 alias 而不是硬编码模型名。这样有两个好处：
>
> 1. 换模型时只改配置不改代码
> 2. 可以按任务复杂度路由——简单查询走 fast 省成本，复杂推理走 pro 保质量
>
> 再加上 `chat_with_template` 方法对接提示词模板系统，整个调用链路从**模板选择 → 模型路由 → 结果返回**是统一的。

### E3-4：统一日志模块

🎓 **面试官问："项目怎么做日志和可观测性？"**

> 我封装了统一的日志工具，基于 Python `logging` 标准库。
> 核心设计是**配置驱动**：日志级别、格式、输出目标都从 `config.yaml` 读取，
> 不同模块通过 `get_logger(__name__)` 获取独立 logger。
> 对于 RAG 评估，我额外加了**结构化日志**——每次问答都**序列化为** JSON，包含 `timestamp`、`answer`、`sources`，方便离线分析和排查 bad case。

---

## E4 — 项目整合与部署

### E4-1：项目整合

🎓 **面试官问："你的项目怎么组织的？有哪些入口？"**

> 我把项目整合成了**统一入口**，通过 `python -m src.app.main` 加上子命令
> 来路由到不同功能：`rag` 做问答、`build` 构建知识库、`serve` 启动 API 服务。
> 同时 `pyproject.toml` 中配置了 `[project.scripts]`，
> `pip install` 之后可以直接用 `my-project rag "问题"` 这样的命令。
> 这意味着我的项目既可以作为**命令行工具**使用，也可以作为**Web 服务**部署。

### E4-2：Docker 容器化

🎓 **面试官问："你的项目能在服务器上跑吗？"**

> 我写了 Dockerfile，基于 `python:3.12-slim`，把依赖安装、源码复制、
> 端口暴露都标准化了。一条 `docker build && docker run` 就能在任何有
> Docker 的环境跑起来。`.env.example` 模板让新成员能快速配置环境变量。

### E4-3：README 与演示素材

🎓 **面试官问："别人能看懂你的项目吗？"**

> 我按开源项目标准写了 README：项目目标、目录结构、快速开始、
> 命令示例、API 文档入口、Docker 部署步骤，以及测试和质量检查命令。
> 一个陌生开发者照着 README 就能在 5 分钟内把项目跑起来。

---

## E6 — 简历素材与面试提纲

### 简历项目描述（可直接放入简历）

**项目名称**：面向文档的 RAG 智能问答系统

**项目周期**：2026 年 7 月

**项目描述**（一段话版本，适合简历"项目经历"栏）：
> 从零构建了一个面向文档的 RAG 检索增强生成问答系统。系统支持 PDF/TXT/MD 等多格式文档导入，通过文本切分、向量化、语义检索和重排精排实现精准的知识库问答。基于 DeepSeek API 实现多模型切换（fast/pro），封装了提示词模板管理系统和结构化日志模块。项目具备 CLI 工具和 FastAPI 接口两种调用方式，配置了 pytest（105 个测试）、ruff、mypy、pre-commit 和 GitHub Actions CI，支持 Docker 一键部署。

**技术栈**：Python 3.12+ / DeepSeek API / FastAPI / SQLAlchemy / sentence-transformers / pytest / ruff / mypy / Docker

---

### STAR 版本（面试时 2 分钟口头讲述用）

**S — Situation（背景）**
> 我在一个月内从 Java/PHP 后端开发转向 Python + AI 方向，目标是产出可展示的 RAG 项目来支撑求职。

**T — Task（任务）**
> 需要从零搭建一个面向文档的智能问答系统，具备完整的 RAG 链路和工程化标准。

**A — Action（行动）**

> **1. 数据处理层**：用 `pdfplumber` + `pandas` 实现多格式文档解析，统一输出结构化 JSON。

> **2. RAG 核心链路**：实现语义切分（固定大小 + 按段落）→ Embedding 向量化 → 余弦相似度检索 → Cross-Encoder 精排 → LLM 生成的完整流程。

> **3. AI 能力增强**：封装模型别名系统（配置文件切换 fast/pro，改配置不改代码）；设计 PromptTemplate + PromptRegistry 模板管理，4 个预置模板支持参数化渲染；统一日志工具支持控制台/文件双输出 + 结构化 JSON 日志。

> **4. 工程化**：105 个 pytest 单元测试，覆盖 prompt_templates(99%)、config(98%)、logging(84%)、chunker(77%) 等核心模块；ruff + mypy + pre-commit 代码质量保障；GitHub Actions CI 自动跑 lint / type check / test；Dockerfile 支持一键部署。

**R — Result（结果）**
> 产出了一个 CLI + API 双入口的完整项目。README 覆盖项目目标、架构、快速开始和部署步骤。能在 5 分钟内向面试官讲清从文档输入到智能回答的完整链路。

---

### 项目亮点（面试时挑 2-3 个展开讲）

| 亮点 | 一句话 | 展开方向 |
|---|---|---|
| 完整 RAG 链路 | 文档→切分→向量→检索→重排→生成 | 讲清楚每步的作用和为什么这样设计 |
| 模型别名系统 | 配置切换 fast/pro，改配置不改代码 | 成本控制：简单任务 fast，复杂任务 pro |
| 提示词模板管理 | 4 个预置模板 + 参数化渲染 + 失败样例 | Prompt 工程化：不再是字符串拼接 |
| 结构化日志 | JSON 格式记录每次 Q&A，含 timestamp/sources | 可观测性：方便排查 bad case 和离线分析 |
| 工程化标准 | 105 测试 + ruff + mypy + CI + Docker | 不是"demo 项目"，是按交付标准写的 |
| 容错设计 | CI 无 API Key 也能跑所有测试 | `LLMClient.__init__` 条件初始化，纯逻辑方法不依赖外部服务 |

---

### 简历技能标签建议

```
Python 3.12+ / RAG 检索增强生成 / LLM API 调用 / Prompt Engineering
FastAPI / SQLAlchemy / Docker / Git / GitHub Actions
pytest / ruff / mypy / pre-commit / CI/CD
pandas / pdfplumber / sentence-transformers
```

---

## E7 — 面试题详细答案

---

### Q1：RAG 的完整链路是什么？每步做什么？

🎓 **面试官问："从用户输入问题到得到回答，你的系统经历了哪些步骤？"**

> RAG 全称是 Retrieval-Augmented Generation（检索增强生成）。
> 核心思路是：**不让 LLM 凭空回答，而是先从知识库中检索相关信息，再把信息作为上下文喂给 LLM 生成回答。**

**完整链路（6 步）：**

```
用户问题
  │
  ▼
① 文档切分（Chunking）：把长文档切成小块，每块 200-500 字符
  │
  ▼
② 向量化（Embedding）：用 sentence-transformers 把每块文本转成向量
  │
  ▼
③ 索引构建：所有向量存入内存数组，形成可检索的知识库
  │
  ▼
④ 召回检索（Retrieval）：用户问题也向量化 → 余弦相似度计算 → 取 top-k 最相关片段
  │
  ▼
⑤ 精排重排（Rerank）：用 Cross-Encoder 对候选片段重新打分排序，取 top-3
  │
  ▼
⑥ 生成回答（Generation）：拼接上下文 + 用户问题 → LLM 生成最终回答
```

**项目实现**：我的 `RAGPipeline.query()` 方法把这 6 步串成一个调用。面试时可以强调"我实现的不是简单的 API 调用，而是理解了每一步的原理并自己写了切分器、检索器和重排器"。

---

### Q2：为什么需要 Chunking？overlap 有什么用？

🎓 **面试官问："为什么不把整篇文档直接丢给 LLM？"**

> 三个原因：**① LLM 有上下文窗口限制**（虽然现在越来越长，但成本随长度平方增长）；
> **② 检索精度**——整篇文档包含太多无关内容，会稀释相关信号，导致检索到的内容不精准；
> **③ 成本控制**——只把真正相关的片段传给 LLM，节省 token 消耗。

**Overlap（重叠窗口）的作用**：

> 如果严格按固定大小切分，可能会在关键信息中间断开。
> 比如"深度学习是机器学习的一个子集"被切成"深度学习是机器"和"学习的一个子集"。
> Overlap 让相邻 chunk 有部分重复内容，**保证不会在关键语义单元中间截断**。
> 我项目中默认 chunk_size=200、overlap=50。

**项目实现**：我实现了两种切分策略——`fixed_size_chunk`（固定大小+句子边界优化+overlap）和 `semantic_chunk`（按段落+句子语义切分）。`TextChunker.__init__` 还加了安全限幅，overlap 不超过 chunk_size 的一半。

---

### Q3：Embedding 召回和 BM25 有什么区别？

🎓 **面试官问："语义检索和关键词检索有什么不同？什么时候用哪个？"**

> **BM25（稀疏检索）**：基于词频统计，查询词在文档中出现次数越多得分越高。
> 优点：精确匹配关键词、可解释性强、不需要训练模型。
> 缺点：不懂同义词——搜"汽车"找不到"轿车"。

> **Embedding（稠密检索）**：把文本映射成 384 维向量，语义相近的文本向量距离近。
> 优点：理解同义词和语义——搜"汽车"能找到"轿车""车辆""四轮交通工具"。
> 缺点：需要模型、计算量大、对专业术语可能不如关键词匹配。

**混合检索（最佳实践）**：

```
用户查询 ─┬─ BM25 关键词匹配 → 候选集 A ─┐
          │                              ├─ 合并去重 → Rerank → LLM
          └─ Embedding 语义匹配 → 候选集 B ┘
```

> 我的项目目前用 **Embedding 检索**（sentence-transformers 多语言模型）。
> 面试时可以说："当前版本用的是 Embedding 语义检索，后续可以加入 BM25 做混合检索，
> 在专业术语和语义理解之间取得平衡。"

---

### Q4：粗排和精排的区别？Cross-Encoder 为什么更准？

🎓 **面试官问："为什么检索完了还要重排？直接取 top-3 不就行了吗？"**

**两阶段架构**：

| | 粗排（召回） | 精排（重排） |
|---|---|---|
| 目标 | 从海量文档中快速筛选候选 | 对候选精细打分排序 |
| 模型 | Bi-Encoder（双编码器） | Cross-Encoder（交叉编码器） |
| 速度 | 快（向量内积，毫秒级） | 慢（需要两两配对计算） |
| 精度 | 中（各自独立编码，无交互） | 高（查询和文档联合编码） |

**Cross-Encoder 为什么更准？**

```
Bi-Encoder（各自编码，无交互）：
  查询 → Encoder → 向量 ┐
                        ├─ 余弦相似度 → 分数
  文档 → Encoder → 向量 ┘

Cross-Encoder（联合编码，充分交互）：
  [查询 + 文档] → Encoder → 分数
```

> Bi-Encoder 单独编码查询和文档，彼此不知道对方的存在，
> 只能通过向量距离间接判断相关性。
> Cross-Encoder 把查询和文档**拼在一起输入**，注意力机制能在两者之间自由交互，
> 直接输出相关性分数，所以更准——但代价是**慢**。

**项目实现**：我的 `Reranker` 优先用本地 BGE Reranker v2-m3（Cross-Encoder），
模型不可用时自动回退到 LLM 打分。面试话术："我用两阶段检索在速度和精度之间取得平衡：
Embedding 粗排从全库召回 5 个候选，Cross-Encoder 精排选出最相关的 3 个给 LLM。"

---

### Q5：怎么评估 RAG 系统好坏？

🎓 **面试官问："你对自己的 RAG 系统做过评估吗？怎么做的？"**

> 评估分三个层次：**自动指标 → 人工评分 → 失败样例驱动优化**。

**1. 自动指标（定量）**：
- 召回率：相关文档有没有被检索到
- MRR（Mean Reciprocal Rank）：正确答案排在第几位
- 这些需要标注数据，小项目通常做不到

**2. 人工评分（定性，我项目中用的方式）**：
- 对每次问答打分（1-5），评估回答是否准确、完整、无幻觉
- 我的 `batch_score.py` 就是做这个的——交互式命令行逐条评分

**3. 失败样例驱动优化（实际有效的方式）**：
- 记录低分 case（≤3 分），分析原因：是检索没召回？重排把对的排后面了？还是 LLM 幻觉？
- 我的 `FAILURE_CATALOG` 记录了 4 个典型失败案例和修复方案

**项目实现**：
```
python -m src.eval.evaluate_rag --generate    # 生成评估日志
python -m src.eval.batch_score               # 人工评分
python -m src.eval.evaluate_rag --analyze    # 查看低分分析报告
```

面试话术："我的评估方式是用结构化日志记录每次问答的 timestamp、answer、sources，
然后人工评分，最后聚焦低分 case 做针对性优化。这比盲目调参数有效得多。"

---

### Q6：怎么管理 Prompt？模板系统怎么设计？

🎓 **面试官问："你们的 Prompt 是硬编码在代码里的吗？"**

> 绝对不是。我设计了一套 **Prompt 模板管理系统**，核心思路是**业务逻辑和提示词文本分离**。

**设计要点**：

> **① 模板数据类（PromptTemplate）**：每个模板有 name、description、version、system_prompt、
> user_template，支持 `{variable}` 占位符。`PromptTemplate.render(**kwargs)` 把占位符替换为
> 实际值，返回 OpenAI 标准消息格式。

> **② 注册中心（PromptRegistry）**：集中管理所有模板。新增模板只需 `registry.register(...)`，
> 不影响现有代码。`registry.render("rag_qa", query="...", context="...")` 一步完成。

> **③ 变量校验**：渲染前检查是否缺少变量，给出明确错误提示（"模板 'rag_qa' 缺少变量: context"），
> 而不是等到 `str.format()` 报 `KeyError`。

> **④ 失败样例驱动迭代**：`FAILURE_CATALOG` 记录了历史上 prompt 出过的问题和修复方案，
> 修改模板时可以回溯，避免重蹈覆辙。

**项目实现**：4 个预置模板（rag_qa、document_summary、relevance_eval、code_review），
配合 `LLMClient.chat_with_template()` 方法，调用链路是：
`模板名 + 变量 → render() → OpenAI 消息格式 → chat() → 回答`。

---

### Q7：多模型切换怎么实现？为什么需要？

🎓 **面试官问："你们用哪个模型？如果哪天想换模型怎么办？"**

> 我在 `LLMClient` 中设计了**模型别名系统**：配置文件中定义 `fast → deepseek-v4-flash`、
> `pro → deepseek-v4-pro`，代码中只传 alias，不硬编码模型名。

**为什么要多模型？**

> **① 成本控制**：简单问答用 fast（便宜），复杂推理用 pro（准确），按需选择。
> 比如 deepseek-v4-flash 比 pro 便宜约 80%，大部分场景够用。
> **② 灰度验证**：新模型上线前用少量流量对比效果，确认没问题再全量切换。
> **③ 容灾回退**：主模型挂了可以切到备用模型，不中断服务。

**三级路由优先级**：
```
1. model 参数直接指定（"gpt-4o"）— 最高优先级，精确控制
2. model_alias 别名查找（"pro" → "deepseek-v4-pro"）— 配置驱动
3. 默认别名（"fast" → "deepseek-v4-flash"）— 开箱即用
```

**项目实现**：`config.yaml` 的 `models.aliases` 段配置映射关系，`LLMClient._resolve_model()`
完成三级解析。`compare_models()` 方法支持同 prompt 多模型对比，切换模型只需改一行配置。

---

### Q8：Agent 和 RAG 的关系？ReAct 模式是什么？

🎓 **面试官问："Agent 和 RAG 有什么区别？你会做 Agent 吗？"**

> **RAG** 是"检索 + 生成"——从知识库中找信息，让 LLM 基于信息回答。它是**被动的**：等待用户问题，返回答案。

> **Agent** 是"推理 + 行动"——LLM 不仅是"回答者"，还是"决策者"：它能**主动选择**调用哪个工具、
> 按什么顺序执行，完成多步复杂任务。它是**主动的**：思考下一步该做什么，然后执行。

**ReAct 模式** = Reasoning + Acting（推理 + 行动）：

```
用户: "北京和上海今天哪个更热？"
  │
  ▼
思考: 我需要先查北京的天气，再查上海的天气，然后比较
  │
  ▼
行动: get_weather("北京")  → "25°C，晴"
  │
  ▼
观察: 北京 25°C
  │
  ▼
行动: get_weather("上海")  → "22°C，小雨"
  │
  ▼
观察: 上海 22°C
  │
  ▼
思考: 北京 25°C > 上海 22°C
  │
  ▼
最终回答: "北京今天更热，25°C 对 22°C。"
```

**项目实现**：`agent_demo.py`（已删除，但面试可以提）实现了两种 Agent：
① 单步 Tool Calling——LLM 根据用户意图自动选择工具；② ReAct Agent——支持
"思考→行动→观察"循环，最多 5 步。

面试话术："RAG 解决'知识在哪'的问题，Agent 解决'怎么做'的问题。
在实际项目中，两者经常结合：Agent 负责拆解任务、调用工具（包括 RAG 检索作为其中一个工具），
然后综合结果给出最终答案。"

---

### Q9：Python 类型注解有什么用？mypy 怎么配？

🎓 **面试官问："你项目里用了类型注解吗？为什么？"**

> 全部用了。Python 3.12+ 的现代语法，比如 `list[dict[str, str]]` 而不是
> `List[Dict[str, str]]`（不再需要 `from typing import List, Dict`）。

**类型注解的好处**：
> ① **IDE 智能提示**——写 `result.` 时自动补全属性，不用查文档
> ② **提前发现 bug**——mypy 在 CI 里跑，类型不匹配直接报错，不会等到运行时才崩
> ③ **代码即文档**——函数签名一看就知道入参和返回值类型

**mypy 配置要点**（`pyproject.toml`）：
```toml
[tool.mypy]
python_version = "3.12"
warn_return_any = false        # 第三方库返回 Any 不报错
ignore_missing_imports = true  # 缺少类型存根的库跳过
check_untyped_defs = true      # 无注解函数也做基础检查

[[tool.mypy.overrides]]
module = ["sentence_transformers", "numpy", "openai"]
ignore_missing_imports = true  # 对 ML 库关闭检查
```

**关键取舍**：对 `sentence_transformers`、`openai` 等第三方 ML 库关闭类型检查——
它们的类型存根不完整，强制检查会导致大量误报，反而掩盖真正的类型错误。

---

### Q10：项目怎么部署？Dockerfile 怎么写？

🎓 **面试官问："你的项目能在服务器上跑吗？别人怎么部署？"**

> 可以，一条命令部署：`docker build -t rag-app . && docker run -p 8000:8000 --env-file .env rag-app`

**Dockerfile 设计要点**：

```dockerfile
FROM python:3.12-slim        # ① slim 镜像：比完整版小 10 倍，减少攻击面
WORKDIR /app

COPY pyproject.toml ./        # ② 分层构建：先复制依赖文件
RUN pip install --no-cache-dir .  # ③ 安装依赖（这层会被缓存）

COPY src/ ./src/              # ④ 最后复制源码（改动频繁，放后面）
COPY config/ ./config/
COPY data/ ./data/

EXPOSE 8000                   # ⑤ 声明端口
CMD ["python", "-m", "src.app.main", "serve", "--host", "0.0.0.0"]
```

**要点解释**：
> **分层构建**：Docker 每行指令是一层，没变的层会缓存。把 `pip install` 放前面——依赖不改时
> 构建秒级完成。源码放后面——只改代码时不需要重装依赖。
> **slim 镜像**：比 `python:3.12` 小约 90%，只有运行时必需的库。
> **host 0.0.0.0**：容器内必须监听 `0.0.0.0` 才能在宿主机访问。

面试话术："我的 Dockerfile 用了分层构建策略，依赖安装和源码复制分开，
日常开发只改代码时构建不到 10 秒。`.env.example` 模板让新成员能快速配置环境变量，
5 分钟内跑起来。"

---

### 加分题：如果上线生产，你还会做什么？

🎓 **面试官问："这个项目如果要上线，你还会加什么？"**

> ① **向量数据库**：当前用内存数组存储向量，生产环境应该换 Chroma/Milvus/Qdrant，支持持久化和十亿级检索。
> ② **混合检索**：Embedding + BM25 混合召回，兼顾问义词语义和关键词精确匹配。
> ③ **多轮对话**：当前只支持单轮问答，加上对话历史管理支持追问。
> ④ **流式输出**：用 SSE 把 LLM 回答逐 token 推给前端，体验更好。
> ⑤ **监控告警**：Prometheus + Grafana 监控 QPS、延迟、token 消耗、错误率。
> ⑥ **A/B 测试框架**：对比不同 chunk_size、top_k、prompt 模板的效果。

---

## E8 — 项目讲稿与最终复盘

### 3-5 分钟项目讲解稿（面试时照着讲或心里过一遍）

---

**开场（30 秒）— 我是谁，做了什么**

> 我做的是一个**面向文档的 RAG 智能问答系统**。
> 简单说就是：上传一批 PDF 或文档，系统能理解内容，你用自然语言提问，它给你精准回答——
> 而且每个回答都能追溯到原文的哪个片段。
>
> 这个项目是我从零搭建的，覆盖了从文档解析、文本切分、向量检索、重排精排到 LLM 生成的完整链路。
> 技术栈是 Python 3.12 + DeepSeek API + FastAPI + sentence-transformers。

---

**架构（60 秒）— 系统怎么工作的**

> 系统分三层：
>
> **第一层是数据处理**：用 pdfplumber 解析 PDF，按页提取文本和元数据，
> 输出结构化 JSON。支持 PDF、TXT、Markdown 多种格式。
>
> **第二层是 RAG 核心**：这是重点。我做了一个完整的检索增强生成链路——
> 文档先进 TextChunker 切分成小块，每块 200 字符、50 字符重叠；
> 然后用 sentence-transformers 多语言模型把每块文本转成 384 维向量；
> 用户提问时，问题也向量化，通过余弦相似度找到最相关的片段；
> 然后经 Cross-Encoder 重排精排，取 top-3 最相关的；
> 最后拼成上下文，调用 DeepSeek 生成回答。
>
> **第三层是工程化**：统一 CLI 入口（`my-project rag/build/serve`），
> FastAPI 提供 HTTP 接口，Docker 一键部署，97 个单元测试，
> ruff + mypy + pre-commit + GitHub Actions CI。

---

**亮点（90 秒）— 我做得好的地方**

> 我想重点讲三个设计决策：
>
> **第一个是模型别名系统**。我设计了一个三级路由机制——
> 配置文件里定义 `fast→deepseek-v4-flash`、`pro→deepseek-v4-pro`，
> 代码里传 alias 不传模型名。这样简单问答走 fast 省成本，
> 复杂推理走 pro 保质量，换模型只改配置不改代码。
>
> **第二个是提示词模板管理**。我没有把 prompt 硬编码在代码里。
> 我设计了 PromptTemplate 数据类和 PromptRegistry 注册中心，
> 每个模板有名字、版本、变量占位符和失败样例记录。
> 调用时用 `registry.render("rag_qa", query="...", context="...")`，
> 自动校验变量完整性，输出标准 OpenAI 消息格式。
>
> **第三个是 CI 容错设计**。我的 LLMClient 在初始化时如果检测不到 API Key，
> 不会崩溃——OpenAI 客户端设为 None，纯逻辑方法（消息验证、模型解析）
> 照样能跑。这意味着 CI 环境不需要配置 API Key 就能跑 97 个测试。

---

**问题和优化（60 秒）— 诚实讲不足，体现思考深度**

> 当前有三个明确的优化方向：
>
> 第一，当前用**内存数组存向量**，适合演示但不适合生产。
> 下一步应该接入 Milvus 或 Qdrant，支持持久化和十亿级检索。
>
> 第二，当前只有 Embedding 召回，**缺少 BM25 关键词召回**。
> 混合检索能让系统在语义理解和精确匹配之间取得平衡。
>
> 第三，评估还依赖**人工评分**，缺少自动指标。
> 如果有标注数据，可以用 MRR、NDCG 做自动化评估。

---

**收尾（30 秒）— 一句话总结**

> 总结一下：我独立完成了一个具备工程化标准的 RAG 系统——
> 从文档解析到智能问答的完整链路，配合 CLI、API、测试、CI 和 Docker。
> 这个项目体现了我同时具备 Python 后端工程化、数据处理和 AI 应用开发的能力。

---

### 最终复盘

#### 本周目标 vs 完成情况

| 阶段 | 目标 | 完成 |
|---|---|---|
| A | Python 基础语法 | ✅ 8/8 任务 |
| B | 工程化（pytest/ruff/mypy/CI/Docker） | ✅ 10/10 任务 |
| C | 数据处理（CSV/Excel/PDF/API/并发） | ✅ 8/8 任务 |
| D | RAG 核心（chunking/embedding/retrieval/rerank/eval/agent） | ✅ 8/8 任务 |
| E | 项目整合 + 求职输出 | ✅ 8/8 任务 |

#### 关键数字

- **97** 个单元测试全部通过
- **10+** 个核心模块
- **4** 个预置 Prompt 模板
- **6** 步完整 RAG 链路
- **1** 份完整 README + 求职话术手册

#### 遇到的问题和解决

| 问题 | 解决 |
|---|---|
| CI 环境无 API Key 导致测试崩溃 | `LLMClient.__init__` 条件初始化，纯逻辑方法不依赖 API |
| 覆盖率 37% | 排除 demo 文件 + 补 38 个测试 → 62% |
| 旧扁平结构残留 32 个文件 | 全面清理，精简为纯 RAG 项目 |
| prompt 硬编码分散在各模块 | 提取为 PromptTemplate + PromptRegistry 统一管理 |

#### 可以继续提升的

- 接入向量数据库（Chroma/Milvus）
- 混合检索（Embedding + BM25）
- 多轮对话支持
- 自动化评估指标
- 流式输出（SSE）

---

### 投递前检查清单

- [x] 项目可运行：`python -m src.app.main --help`
- [x] 测试全绿：`pytest tests/` → 97 passed
- [x] 代码规范：`ruff check` + `mypy` 无报错
- [x] README 完整：项目目标、架构、快速开始、API 示例、Docker 部署
- [x] 简历项目描述：一段话 + STAR 版本 + 技术栈
- [x] 面试题准备：10 道高频题 + 答案 + 加分题
- [x] 项目讲稿：3-5 分钟，覆盖架构、亮点、问题和优化
- [x] 求职话术手册：A 到 E 阶段全覆盖
