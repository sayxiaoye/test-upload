---
description: 项目通用准则：中文沟通与最小改动原则
alwaysApply: true
---

# Project Guidelines

## 沟通

- 始终用中文回复用户。
- 解释清晰、简洁；复杂问题可结构化，但不要堆砌无关内容。

## 改动范围

- **最小 diff**：只改完成任务所需的代码，不动无关文件。
- **避免过度设计**：不提前抽象、不增加「可能有用」的 helper、配置或层级。
- **不擅自扩展**：未明确要求时，不添加功能、测试、文档或 refactor。

## 代码风格

- 先读周边代码，匹配现有命名、结构与抽象层次。
- 注释只解释非显而易见的业务或技术细节；代码本身应可读。
- 仅在用户明确要求，或能覆盖真实行为时，才添加测试。

## 示例

```typescript
// ❌ BAD：为单行逻辑抽 helper
function isEmpty(s: string) { return s.length === 0; }

// ✅ GOOD：简单逻辑就地写
if (name.length === 0) return;
```
